% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kwic.R
\name{kwic}
\alias{kwic}
\title{Keyword-in-context concordance}
\usage{
kwic(
  phrase,
  pattern,
  window = 5L,
  div = c("morph", "words", "nouns"),
  fixed = TRUE,
  sys_dic = "",
  user_dic = "",
  parallel = FALSE
)
}
\arguments{
\item{phrase}{A character vector or list of character scalars.}

\item{pattern}{A single search term. Matched exactly against a morpheme when
`fixed = TRUE`, or as a regular expression when `fixed = FALSE`.}

\item{window}{Number of context tokens to show on each side.}

\item{div}{Token preset used to segment the text.}

\item{fixed}{Match `pattern` exactly against a token; when `FALSE`, treat it
as a regular expression.}

\item{sys_dic}{Optional Korean system dictionary directory.}

\item{user_dic}{Optional compiled user dictionary.}

\item{parallel}{Use parallel analysis for multiple documents.}
}
\value{
A data frame with columns `doc`, `position` (token index of the
  match), `left`, `keyword`, and `right`. Left and right context tokens are
  joined with single spaces.
}
\description{
Finds occurrences of a keyword among the morphemes of each document and
returns them with their left and right context, the Korean analogue of a
classic KWIC concordance. Matching is done on morpheme tokens, so it is
robust to the particles and endings that attach to a word in running text.
}
\examples{
\dontrun{
docs <- c("\ud55c\uad6d\uc5b4 \ubd84\uc11d\uc740 \uc990\uac81\ub2e4",
          "\ud55c\uad6d\uc5b4 \uacf5\ubd80\ub294 \uc5b4\ub835\ub2e4")
kwic(docs, "\ud55c\uad6d\uc5b4")
}
}
